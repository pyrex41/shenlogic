# PO6 THF

**Obligation.** Typed decls, ctor axioms, and simultaneous SCC leastness quantify over the complete candidate model. No finite sample is a proof.

**Notes.** Emitter is full-model. TPTP4X is factorial syntax only. No simultaneous-SCC THF golden. Lean encoding lemmas only.

**Code.** None yet (`mutual.thf` missing).

**Plan.** 1) Add `tests/golden/mutual.thf` + `mutual-thf` sl-check on `examples/mutual.shen`. 2) Optionally TPTP4X-parse it in `backend-check`. 3) No Lean proof of PO6.
